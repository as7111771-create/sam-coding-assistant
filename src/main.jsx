import React from "react";
import ReactDOM from "react-dom/client";
import SamChat from "./SamChat.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SamChat />
  </React.StrictMode>
);
